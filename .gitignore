# Python / fabric-cicd
.venv/
venv/
__pycache__/
*.pyc
.env
*.env

# fabric-cicd / deployment local state
deploy/.fabric-cicd/
*.log

# Local secrets — never commit
**/secrets.*
local.settings.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# Power BI local artifacts (we keep PBIR source, not binaries)
*.pbix
*.pbids
